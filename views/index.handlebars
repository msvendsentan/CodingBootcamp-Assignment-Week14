<header>
    <h1>To-Do List</h1>
</header>

<div id="instructions">
    <p>Set up reminders for important tasks. You can mark tasks as complete or incomplete, or simply delete them.</p>
</div>

<div id="lists">
    <div class="list">
        <h2>Incomplete</h2>
        <table>  
            {{#each tasks}}
                {{#unless complete}}
                    <tr>
                        <td>{{this.task}}</td>
                        <td><button class="complete" data-id={{this.id}} title="Mark as Complete"><i class="fas fa-check"></i></button></td>
                        <td><button class="delete" data-id={{this.id}} title="Delete Task"><i class="fas fa-times"></i></button></td>
                    </tr>
                {{/unless}}
            {{/each}}
        </table>
    </div>
    <div class="list">
        <h2>Complete</h2>
        <table>
            {{#each tasks}}
                {{#if complete}}
                    <tr>
                        <td>{{this.task}}</td>
                        <td><button class="incomplete" data-id={{this.id}} title="Mark as Incomplete"><i class="fas fa-arrow-left"></i></button></td>
                        <td><button class="delete" data-id={{this.id}} title="Delete Task"><i class="fas fa-times"></i></button></td>
                {{/if}}
            {{/each}}
        </table>
    </div>
</div>
<div id="submitwindow">
    <h2>Set a Reminder</h2>
    <form>
        <input type="text" id="task" placeholder="Enter a task...">
        <input type="submit" id="submit" value="Submit">
    </form>
</div>
<div id="api">
    <a href="/api/all">API</a>
</div>